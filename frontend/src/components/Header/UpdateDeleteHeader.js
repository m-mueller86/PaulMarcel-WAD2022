import React from "react";

import "./UpdateDeleteHeader.css"

const UpdateDeleteHeader = ({ user }) => {
    return (
        <header className="updateDelete-header">
            <h2>Update & Delete Location</h2>
        </header>
    );
};

export default UpdateDeleteHeader;