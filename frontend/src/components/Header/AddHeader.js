import React from "react";

import "./AddHeader.css"

const AddHeader = () => {
    return (
        <header className="add-header">
            <h2>Add Location</h2>
        </header>
    );
};

export default AddHeader;